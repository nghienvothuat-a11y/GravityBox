# Boss geometry previews

Rendered from Generation 8 authored prefabs at the initial ball poses, with the project camera and rendering pipeline. These are inspection fixtures, not screenshots of solved routes. All 100 previews plus top views are available locally in `Artifacts/Campaign100`.
